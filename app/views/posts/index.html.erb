<div class="row">
	<%= @posts.each do |post| %>
		<div class="columns small-12 medium-4">
			<div class="panel">
				<img src="<%= post.image %>" alt="<%= post.caption %>">
				<h2><%= post.caption %></h2>
			</div>
		</div>
	<% end %>
</div>


	

